//점검모드 켜기
db.ref('maintenanceMode').update({ enabled: true })

//점검모드 끄기
db.ref('maintenanceMode').update({ enabled: false })

//현재 상태 확인
db.ref('maintenanceMode').once('value').then(s => console.log(s.val()))